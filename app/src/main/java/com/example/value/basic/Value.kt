package com.example.value.basic

class value {
    fun main(){
        var a : String = "";
        a = "phat";
        val b : String = "phat"
        // khi dùng val chỉ để laays dữ liệu là chỉ được đọc , giống final

        // ép kiểu : ```a.to```
        a.toInt()
    }
}